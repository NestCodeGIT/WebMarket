@using Webmarket.Modelss
@model  IEnumerable<Company>
@{
    ViewData["Title"] = "Index";
}
<partial name="_Notification" />

<link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />


<div class="container mt-3 bg-danger border shadow-lg">

    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-white"> List Category </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Company" asp-action="Create" class="btn btn-outline-primary"> <i class="bi bi-plus-circle-fill"></i> Create new Company</a>
        </div>

    </div>
  
  <p></p>            
      <table class="table table-dark table-striped table-bordered table-hover shadow-lg ">
        <thead>
              <tr >
                    <th> Name Company</th>
                    <th>TelefonNummer</th>
                    <th></th>
                    <th>

                    </th>
                    
              </tr>
        </thead>
            <tbody  >
                        @foreach (var obj in Model)
                    {
                          <tr >
                            <td >@obj.Name</td>
                           
                            <td>@obj.PhonNumber</td>
                          
                              <td>
                                  <div class="w-75 btn-group" role="group">
                                      <a class="btn btn-outline-danger"     asp-controller="Company" asp-action="Edit" asp-route-id="@obj.Id" ><i class="bi bi-pencil-square"></i>  Edit</a>
                                               <a class="btn btn-outline-warning" asp-controller="Company" asp-action="delete" asp-route-id="@obj.Id" >
                                                   <i class="bi bi-trash3"></i> Del
                                                </a>
                                  </div>
                              </td>
                          </tr>
                       
                    }
                
            </tbody>
      </table>
</div>